select * from (
 select 1 序号,1 子序号, 科室, 项目, 人次, 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select b.名称 科室,
                 项目,
                 sum(人次) as 人次,
                 sum(数次) as 数次
            from tab a, 部门表 b
           where a.开单部门id = b.id
             and 时间 between [0] and [1]
           group by b.名称, 项目)
 union all
 select 1 序号,2 子序号, 科室, 项目, 人次, 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select 名称 科室,
                 '中医治疗参与率' 项目,
                 decode(sign(round(a.人次 / b.人次 * 100, 2) - 100),
                        1,
                        100,
                        round(a.人次 / b.人次 * 100, 2)) 人次,
                 decode(sign(round(a.人次 / b.人次 * 100, 2) - 100),
                        1,
                        100,
                        round(a.人次 / b.人次 * 100, 2)) 数次
            from (select 开单部门id, sum(人次) 人次, sum(数次) as 数次
                    from tab
                   where 时间 between [0] and [1]
                   group by 开单部门id) a,
                 (select 科室id, sum(A.已挂数) as 人次
                    from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                         部门表 B,
                         收费项目目录 C,
                         (select sum(人次) as 合计
                            from (select b.名称 as 科室,
                                         C.名称 as 挂号项目,
                                         sum(A.已挂数) as 人次
                                    from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                                         部门表       B,
                                         收费项目目录 C
                                   where A.日期 between [0] and [1]
                                     and B.ID = A.科室ID
                                     and C.ID = A.项目ID
                                     and C.类别 = '1'
                                   group by b.名称, C.名称)) D
                   where A.日期 between [0] and [1]
                     and B.ID = A.科室ID
                     and C.ID = A.项目ID
                     and C.类别 = '1'
                   group by 科室id
           having sum(A.已挂数)<>0) b,
                 部门表 c
           where a.开单部门id = b.科室id
             and a.开单部门id = c.id)
 union all
 select 1 序号,3 子序号, 科室, 项目, 人次, 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select 名称 科室,
                 '非药物治疗参与率' 项目,
                 decode(sign(round(a.人次 / b.人次 * 100, 2) - 100),
                        1,
                        100,
                        round(a.人次 / b.人次 * 100, 2)) 人次,
                 decode(sign(round(a.人次 / b.人次 * 100, 2) - 100),
                        1,
                        100,
                        round(a.人次 / b.人次 * 100, 2)) 数次
            from (select 开单部门id,
                         sum(case
                               when 项目 = '中药人次' then
                                0
                               else
                                人次
                             end) 人次,
                         sum(case
                               when 项目 = '中药人次' then
                                0
                               else
                                数次
                             end) as 数次
                    from tab
                   where 时间 between [0] and [1]
                   group by 开单部门id) a,
                 (select 科室id, sum(A.已挂数) as 人次
                    from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                         部门表 B,
                         收费项目目录 C,
                         (select sum(人次) as 合计
                            from (select b.名称 as 科室,
                                         C.名称 as 挂号项目,
                                         sum(A.已挂数) as 人次
                                    from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                                         部门表       B,
                                         收费项目目录 C
                                   where A.日期 between [0] and [1]
                                     and B.ID = A.科室ID
                                     and C.ID = A.项目ID
                                     and C.类别 = '1'
                                   group by b.名称, C.名称)) D
                   where A.日期 between [0] and [1]
                     and B.ID = A.科室ID
                     and C.ID = A.项目ID
                     and C.类别 = '1'
                   group by 科室id
           having sum(A.已挂数)<>0) b,
                 部门表 c
           where a.开单部门id = b.科室id
             and a.开单部门id = c.id)

 union all

 select 2 序号,1 子序号, '总合计' 科室, 项目, sum(人次) 人次, sum(数次) 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select 项目,
                 sum(人次) as 人次,
                 sum(数次) as 数次
            from tab a, 部门表 b
           where a.开单部门id = b.id
             and 时间 between [0] and [1]
           group by 项目)
 union all
 select 2 序号,2 子序号, '总合计' 科室, '中医治疗参与率' 项目,
        round(sum(a.人次) / sum(b.人次) * 100, 2) 人次,
        round(sum(a.人次) / sum(b.人次) * 100, 2) 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select 开单部门id, sum(人次) 人次, sum(数次) as 数次
                    from tab
                   where 时间 between [0] and [1]
                   group by 开单部门id) a,
                (select 科室id, sum(A.已挂数) as 人次
                   from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                        部门表 B,
                        收费项目目录 C,
                        (select sum(人次) as 合计
                           from (select b.名称 as 科室,
                                        C.名称 as 挂号项目,
                                        sum(A.已挂数) as 人次
                                   from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                                         部门表       B,
                                         收费项目目录 C
                                   where A.日期 between [0] and [1]
                                     and B.ID = A.科室ID
                                     and C.ID = A.项目ID
                                     and C.类别 = '1'
                                   group by b.名称, C.名称)) D
                  where A.日期 between [0] and [1]
                    and B.ID = A.科室ID
                    and C.ID = A.项目ID
                    and C.类别 = '1'
                  group by 科室id
          having sum(A.已挂数)<>0) b,
                部门表 c
          where a.开单部门id = b.科室id
            and a.开单部门id = c.id
 union all
 select 2 序号,3 子序号, '总合计' 科室, '非药物治疗参与率' 项目,
        round(sum(a.数次) / sum(b.人次) * 100, 2) 人次,
        round(sum(a.数次) / sum(b.人次) * 100, 2) 数次
   from (with tab as (select 开单部门id,
                             时间,
                             '整复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and (c.id = 264 or b.名称 like '石膏固定术%')
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '筋伤次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and b.id = 2648
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '针灸次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 59
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '推拿次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 60
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '康复次数' 项目,
                             count(distinct 病人id) 人次,
                             sum(数次) 数次
                        from (select 病人科室id 开单部门id,
                                     病人id,
                                     max(数次) as 数次,
                                     trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a,
                                     收费项目目录 b,
                                     收费分类目录 c
                               where a.收费细目id = b.id
                                 and b.分类id = c.id
                                 and 记录状态 <> 0
                                 and c.id = 57
                                 and a.记录状态 <> 0
                               group by 病人科室id, 病人id, 登记时间)
                       group by 开单部门id, 时间
                      union all
                      select 开单部门id,
                             时间,
                             '中药人次' 项目,
                             count(distinct 病人id) 数次,
                             count(distinct 病人id) 人次
                        from (select distinct 病人科室id 开单部门id,
                                              病人id,
                                              trunc(登记时间, 'month') 时间
                                from 门诊费用记录 a, 收费项目目录 b
                               where a.收费细目id = b.id
                                 and 记录状态 <> 0
                                 and 收费类别 in ('6','7')
                                 and a.记录状态 <> 0)
                       group by 开单部门id, 时间 )
          select 开单部门id,
                         sum(case
                               when 项目 = '中药人次' then
                                0
                               else
                                人次
                             end) 人次,
                         sum(case
                               when 项目 = '中药人次' then
                                0
                               else
                                数次
                             end) as 数次
                    from tab
                   where 时间 between [0] and [1]
                   group by 开单部门id) a,
                (select 科室id, sum(A.已挂数) as 人次
                   from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                        部门表 B,
                        收费项目目录 C,
                        (select sum(人次) as 合计
                           from (select b.名称 as 科室,
                                        C.名称 as 挂号项目,
                                        sum(A.已挂数) as 人次
                                   from (select trunc(A.发生时间) as 日期,A.执行部门id as 科室id,B.项目ID,sum(decode(A.记录状态,1,1,3,1,2,-1))  as 已挂数
  from 病人挂号记录 A,挂号安排 B
   where A.发生时间 between  [0] and [1]
  and A.记录状态<>0
  and A.号别=B.号码(+)
  group by trunc(A.发生时间),A.执行部门id,B.项目ID
  order by trunc(A.发生时间),A.执行部门id,B.项目ID) A,
                                         部门表       B,
                                         收费项目目录 C
                                   where A.日期 between [0] and [1]
                                     and B.ID = A.科室ID
                                     and C.ID = A.项目ID
                                     and C.类别 = '1'
                                   group by b.名称, C.名称)) D
                  where A.日期 between [0] and [1]
                    and B.ID = A.科室ID
                    and C.ID = A.项目ID
                    and C.类别 = '1'
                  group by 科室id
          having sum(A.已挂数)<>0) b,
                部门表 c
          where a.开单部门id = b.科室id
            and a.开单部门id = c.id
) order by 序号, 子序号 asc, 数次 desc
